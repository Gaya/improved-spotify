declare global {
  enum PlaylistView {
    PLAYLIST = 'PLAYLIST',
    ALBUM = 'ALBUM',
    ARTIST = 'ARTIST',
  }

  export enum TrackState {
    VALID = 'VALID',
    INVALID = 'INVALID',
  }
}

export {};
